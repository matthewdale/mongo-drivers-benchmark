# mongo-drivers-benchmark

Benchmarking MongoDB drivers.

## HTTP API spec

See [`spec/http-api.md`](./spec/http-api.md) (prose, source of truth for semantics) and [`spec/openapi.yaml`](./spec/openapi.yaml) (OpenAPI 3.1, source of truth for shape) for the shared HTTP API that per-driver load-test services implement.
